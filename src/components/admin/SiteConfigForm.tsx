'use client'

// This component is kept for potential future use but the configuration
// is handled directly in the admin/configuracion page
export default function SiteConfigForm() {
  return null
}
